# 代码稳定性体检报告

- **日期**：2026-07-14
- **项目**：华为云解决方案智能匹配系统（cloudsol.cn）
- **版本**：v20260714bd（知识库路由鉴权修复 + LLM 超时收紧）
- **环境**：本地 Windows / Python 3.13 / FastAPI + 原生 JS SPA + SQLite + ChromaDB

---

## 一、总体结论

**代码整体稳定，可以正常运行与上线。** 语法编译、后端启动、路由可达、数据库与知识库初始化、AI 链路兜底全部通过，未发现会导致崩溃的缺陷。

发现 **2 个一致性/可用性隐患 + 1 个体验优化点**，均属非阻塞级别，建议后续择机修复。

---

## 二、体检清单（8 项全部通过）

| # | 检查项 | 方法 | 结果 |
|---|--------|------|------|
| 1 | 后端 Python 编译 | `python -m compileall api` | ✅ 全部通过 |
| 2 | 前端 JS 语法 | `node --check` 对 script.js / welcome-script.js / js/*.js | ✅ 全部通过 |
| 3 | 后端启动冒烟 | 拉起 uvicorn（端口 8012）| ✅ ~10s 完成（含向量模型预热），`/docs`、`/` 均 200 |
| 4 | 数据库初始化 | 启动日志 | ✅ DB 初始化完成 |
| 5 | 知识库加载 | 启动日志 | ✅ 277 片段 / 44 华为文件 / 120 竞品 / 10 行业 / 覆盖度 89% |
| 6 | AI 链路兜底 | 源码审查 routes.py + llm.py | ✅ 三路路由 + history 边界 + kb 异常兜底 + LLM 超时/重试 |
| 7 | 全局异常处理 | main.py `global_exception_handler` | ✅ 异常转 500 JSON，不会裸崩 |
| 8 | 边界请求 | curl 实测 | ✅ 空问题→400、dashboard 无 token→403 |

---

## 三、发现的问题

### 问题 1【中 · 鉴权一致性】— ✅ 已修复（2026-07-14）
- **现象**：`GET /api/knowledge/documents`（文档列表）未强制登录，匿名请求返回 `200`，并泄露 171 篇文档的标题 / 竞品 / 来源元数据（不含正文）。
- **冲突**：与既定约定"知识库路由强制登录"不符。
- **影响**：信息泄露（低风险——无正文，且资料本就半公开），但属一致性 bug。
- **修复**：`api/routes.py` 的 `list_knowledge_documents` 与 `get_knowledge_document` 两路由 `Depends(get_current_user_optional)` → `Depends(get_current_user)`；匿名访问实测已从 `200` → `403`。前端 `KnowledgeUI.loadDocList` 增加登录守卫：未登录直接显示"请登录后查看知识库文档"，不再发请求、不报错。

### 问题 2【低-中 · 可用性】— ✅ 已修复（2026-07-14）
- **现象**：LLM 调用配置 `REQUEST_TIMEOUT=120s` × `MAX_RETRIES=3`，最坏情况下单次 AI 请求可卡约 **6 分钟**并长期占用连接。
- **影响**：DeepSeek API 抖动时，并发请求会被拖垮，其他用户请求排队。
- **修复**：因 `.env` 被 gitignore（不在版本库），仅改本地 `.env` 部署后不生效；故**直接收紧 `app/config.py` 默认值** `REQUEST_TIMEOUT 120→60`、`MAX_RETRIES 3→2`（已验证 `from app.config import` 读到 60/2），本地与服务器均生效；并同步 `.env.example` 为 60/2。最坏卡顿从 ~6 分钟降至 ~2.4 分钟。部署若服务器 `.env` 显式写了 `REQUEST_TIMEOUT` 旧值需手动更新。

### 问题 3【低 · 体验】
- **现象**：AI 调用失败时返回 `HTTP 500` + 文案。前端 `catch` 会提示用户，不会崩，但 500 易被误判为"系统故障"。
- **修复建议（可选）**：改为返回 `200` + 友好兜底文案（如"AI 助手暂时开小差，请稍后再试"）。

---

## 四、链路健壮性细节（已确认无隐患）

- **AI 三路路由**：平台使用 / 云业务 RAG / 通用问答，关键词命中逻辑清晰，无遗漏分支。
- **对话历史**：取最近 20 条（10 轮），`recent_history[:-1]` 在空列表时返回 `[]` 不报错。
- **知识库检索异常**：`_answer_business_question` 中 `context_text` 在 `try` 外定义兜底，检索失败走"暂无相关资料"分支，不会 `NameError`。
- **LLM 超时保护**：`httpx.Timeout(connect=10, read=120, ...)` + `_retry_request` 重试，不会无限挂起。
- **前端 fetch**：主流请求均包裹 `try/catch` 或 `.catch()`，单点失败不会全局崩溃。

---

## 五、处理状态

- ✅ **问题 1**（知识库路由强制登录）：已修复并验证（匿名 `200`→`403`）
- ✅ **问题 2**（LLM 超时收紧）：已修复（`.env` 加 `REQUEST_TIMEOUT=60`、`MAX_RETRIES=2`）
- ⬜ **问题 3**（AI 失败返回码优化）：仍可选，纯体验，未动

> 注：问题 1、2 修复后已通过本地冒烟验证（匿名→403、服务正常、前端语法 OK）。部署前需重启后端 + 前端硬刷新（前端版本已升 `v20260714bd`）。
