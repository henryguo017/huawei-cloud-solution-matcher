# 部署前就绪度处理报告（2026-07-14）

> 代码/配置层部署前隐患已修复并推送 `23480e8`（最新 main）。

## 一、已修复的 4 项（已推送）

| # | 类型 | 问题 | 修复 |
|---|------|------|------|
| 1 | 部署效率/冲突 | `requirements.txt` 残留 `streamlit`/`pandas`/`plotly`，三者仅被遗留的 Streamlit 原型 `app/main.py` 使用，FastAPI 后端完全不依赖 → 部署安装多几百 MB、且有版本冲突风险 | 从 `requirements.txt` 移除这三个包（已确认无代码 import `app.main`） |
| 2 | **安全·高危** | `app/config.py` 把 163 邮箱 SMTP 授权码 `SMTP_PASS="SMyjF34BjgCFMGud"` 硬编码为默认值 → 公开 GitHub 仓库已泄露该凭据 | 改为 `os.getenv("SMTP_PASS", "")`，凭据只放 `.env`（已被 gitignore） |
| 3 | 安全 | `.env.example` 缺 `JWT_SECRET_KEY`/SMTP 等关键变量 → 若服务器从它生成 `.env`，会用代码里的弱默认密钥，令牌可被伪造 | 补全 `JWT_SECRET_KEY`（占位值会触发部署脚本的"请修改默认配置"警告）、`SMTP_*`、`RESET_TOKEN_EXPIRE_MINUTES`、`FRONTEND_URL`、`DATABASE_URL` |
| 4 | 卫生 | `deploy/server-deploy.sh` 仍指向已废弃旧服务器 `101.37.28.206` | 改为新服务器 `47.96.109.234`（实际部署走 `update.sh`，此脚本为一次性全装脚本，纠正防误部署） |

## 二、⚠️ 部署前后你必须处理的（按优先级）

1. **立即重置 163 邮箱授权码**：旧授权码 `SMyjF34BjgCFMGud` 已写在公开 git 历史里，任何人都能看到。请登录 163 邮箱 → 设置 → 客户端授权码，重新生成并替换（同时更新服务器 `.env` 的 `SMTP_PASS`）。
2. **部署后确认服务器 `.env` 含两项关键配置**：
   - `JWT_SECRET_KEY=` 随机强密码（否则用弱默认密钥，登录令牌可被伪造）
   - `SMTP_PASS=` 上面重置后的新授权码
   - 注：`update.sh` **不会覆盖**服务器已有 `.env`；若之前没配过，需手动 `nano /var/www/huawei-cloud-solution-matcher/.env` 补上。
3. **改默认管理员密码**：系统初始化会建 `admin / admin123`，上线前应改掉（日志也有此警告）。
4. **孤儿进程 PID 7488**（你一会处理）：本机/服务器上多跑的 uvicorn 进程，部署前清理避免抢 ChromaDB 文件锁。

## 三、确认无问题的项（未动）

- `db_init.py`：全部表 `CREATE TABLE IF NOT EXISTS`，`users` 表 `token_version`/`reset_token`/`reset_token_expiry` 均有幂等 `ALTER`（try/except 忽略已存在）—— 严守"部署只覆盖代码不碰 DB"的迁移铁律。
- `deploy/huawei-cloud-api.service`：启动命令 `python -m uvicorn api.main:app` 正确（非会出错的 `python -m api.main`）。
- `deploy/update.sh`：部署链路正确（新服务器 IP + wget GitHub zip + 排除 `data/` 保留数据库 + 重启服务）。
- 前端版本号已最新：`script.js?v=20260714bf`、`welcome-script.js?v=20260714j`，部署后能正确破浏览器缓存。
- `chromadb==0.4.24` 已钉死，与现有生产库（无 topic 列）已验证兼容，部署无需改动。

## 四、建议后续清理（不影响本次部署）

- `app/main.py` 是早期 Streamlit 原型，已被 FastAPI 后端取代，现在引用已移除的 streamlit 包，属于死代码。建议日后删除，避免误解。
