# L4-P0 计划闭合门 · 活测验收记录（2026-09-13）

> 目的：回答两个离线脚本答不了的问题 —— ① 真模型会不会**自己**把计划闭合（还是只会一路检索到底）？② 出现未闭合时，宿主拦截后模型能不能**自己**补救？
> 结论：**纪律成立**；首轮（n=5）主口径 0.750 的唯一失败源于**宿主预算熔断**而非模型纪律 → 据此重标定 token 预算并扩测到 **n=12 复验，主口径 1.000（8/8）达标**。本轮另发现并修复熔断路径的收口缺口。

---

## 1. 方法

| 项 | 值 |
|---|---|
| 脚本 | 首轮 `.workbuddy/Temp/p0_closure_live.py`（n=5）；扩测 `.workbuddy/Temp/p0_closure_12q.py`（n=12）；定向探针 `p0_fuse_probe.py` |
| 端点 | `POST /api/agent/chat`，`runtime=fc` + `autonomy=high`（双门禁） |
| 模型 | DeepSeek（真实调用，非 mock） |
| 观测 | SSE `plan` / `tool_start` / `result` 事件；`fc_meta.{turns,stopped_by,drift_rejections,plan_open_at_final,plan_steps}` |
| 原始日志 | `.workbuddy/Temp/p0_live.log` |

指标口径：
- **A11 主口径** ＝ 交付时计划全闭合的 FC 运行 ÷ 发布过计划（≥2 步）的 FC 运行（含被宿主熔断的样本，**不剔除、不粉饰**）
- **A11 参考口径** ＝ 同式，但**只统计模型自主终止**（`stopped_by=model`）的样本 —— 用于区分「模型不做」与「宿主没让它做完」

---

## 2. 逐例结果（首轮 n=5；扩测见 §6）

| # | 类别 | 任务 | 引擎 | 耗时 | 轮次 | 终止 | 计划步数 | 交付时未闭合 | 闭合门拦截 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | solution | 300 人跨境电商上云方案 | **legacy** | 88.4s | — | — | — | — | — |
| 2 | solution | 智慧水务（担心数据安全） | fc | 30.3s | 5 | model | 5 | 0 | 0 |
| 3 | solution | 高校一卡通 + 机房改造规划 | fc | 29.6s | 6 | model | 6 | 0 | 0 |
| 4 | competitor | 华为云 vs 阿里云（制造业物联网） | fc | 46.8s | 9 | **tokens** | 4 | **1** | **1** |
| 5 | competitor | 腾讯云智慧医疗动态调研 | fc | 31.5s | 8 | model | 4 | 0 | 0 |

未进入 FC 的 1 例（#1）由范围门按意图分流至老管线 —— 属既定设计（D5），不计入 A11。

---

## 3. A11 结论（首轮 n=5，扩测见 §6）

```
有效运行 5  |  进入 FC 4  |  宿主熔断 1
发布计划(≥2步) 4  |  交付时计划全闭合 3
闭合门累计拦截 1 次

A11 计划收敛率（主口径·全部 FC 运行） = 0.750（目标 ≥0.90） → FAIL ❌
A11（参考·仅模型自主终止）          = 1.000（3/3）  ← 仅参考，不替代主口径
```

**怎么读这个 0.750：**

- 参考口径 **1.000** 是本次最重要的发现：模型**自发**发布计划、自发闭合（含**如实标 `skipped`**，例 5 把「联网检索」「抽取网页正文」两步标 skipped 并在终稿说明）。**不是提示词逼出来的**。
- 主口径唯一的失败样本（#4）终止原因是 `stopped_by=tokens` —— 模型在 9 轮 / 13 次工具调用后**被宿主预算掐断**，客观上没机会闭合最后一步。**这是宿主容量问题，不是模型的纪律问题。**
- 闭合门**实证生效**：#4 `drift_rejections=1` —— 模型曾试图带着未闭合步交付，被宿主拦下、退回自决。

---

## 4. 活测发现并已修复的缺口：熔断路径绕过闭合门

**问题**：熔断（预算/轮次到顶）走的是「宿主索要终稿」分支，该分支此前**不经过闭合门** → 模型可能带着未闭合步直接出终稿，且**宣称已完成**。

**修法**（`app/agent/runtime/runner.py`，提交 `3b872b0`）：熔断收口时若 `open_steps(harness._plan_status)` 非空，则在收口指令后追加：

> （宿主补充）注意：你计划里仍有未闭合的步骤（…）。本次因预算/轮次已到上限无法继续执行，请务必在答案中**如实说明**哪些部分尚未完成或未核实，不要谎称已经完成。

**边界（关键）**：不改 A11 口径（该样本**如实算失败**）、不改执行顺序、不做「工具属于哪一步」的步归属推导 —— 宿主只做核验，不替模型粉饰。

**验证**：
- 离线：`fc_runtime_smoke.py` **48/48**（新增 T10c 断言注入文本 + 终稿如实说明；T10b 断言超限后不死锁）。
- 活测：定向探针 `.workbuddy/Temp/p0_fuse_probe.py` 复跑同题 → `stopped_by=tokens` + `plan_open_at_final=2` → 终稿出现**「七、待补充与未核实事项（如实说明）」**小节，列出 4 条**真实**缺口（联网通道关闭 / 未调成本工具 / 未写入客户档案 / 阿里云侧颗粒度弱）。逻辑闭环：熔断 + `open>0` ⟹ 必然注入。

---

## 5. 验证边界（诚实口径）

本轮**无法**从服务器日志直接取证「注入文本」。原因（已读码确认，非猜测）：

1. `app/agent/agent.py:44` 默认 `verbose=False` → `harness._log()` 只写入内存 `self._logs`，不落盘（试过 `--log-level info` 也无效 —— 卡点是 verbose，不是日志级别）。
2. `api/agent_routes.py:381-395` 的 `result` SSE 事件是**字段白名单**，**不含 `logs`**。

因此证据链为：**白名单内的 `fc_meta`（`stopped_by` + `plan_open_at_final` + `drift_rejections`）＋ 离线 T10c ＋ 终稿正文**。三者互证，足以判定修补生效。

---

## 6. 预算重标定与扩测复验（n=12）

**动机**：首轮 n=5 的 A11 主口径 0.750 只有 4 个 FC 样本，1 例熔断就能让比率剧烈跳动 —— 样本量不足以下判，且熔断根因需要数据而非估算来定位。

**做法**：把 FC 可进入的两类意图跑满（solution 6 + competitor 6 = 12 例），并把每例真实 `fc_meta.tokens` 打出来，用**实测消耗分布**定预算档位。

### 6.1 结果（预算 400k）

```
有效运行 12  |  进入 FC 8  |  宿主熔断 0
发布计划(≥2步) 8  |  交付时计划全闭合 8
闭合门累计拦截 0 次

A11 计划收敛率（主口径·全部 FC 运行） = 1.000（8/8） → PASS ✅
A11（参考·仅模型自主终止）          = 1.000（8/8）
```

| # | 类别 | 引擎 | 轮 | 终止 | tokens | 计划步 | 未闭合 | 工具 |
|---|---|---|---|---|---|---|---|---|
| 1 | solution | legacy | — | — | — | — | — | 14 |
| 2 | solution | fc | 5 | model | 85,464 | 4 | 0 | 8 |
| 3 | solution | fc | 6 | model | 95,465 | 5 | 0 | 6 |
| 4 | solution | legacy | — | — | — | — | — | 0 |
| 5 | solution | legacy | — | — | — | — | — | 6 |
| 6 | solution | fc | 5 | model | 63,464 | 5 | 0 | 4 |
| 7 | competitor | fc | 12 | model | **316,417** | 4 | 0 | **17** |
| 8 | competitor | fc | 8 | model | 81,385 | 4 | 0 | 9 |
| 9 | competitor | fc | 5 | model | 78,673 | 5 | 0 | 6 |
| 10 | competitor | legacy | — | — | — | — | — | 15 |
| 11 | competitor | fc | 5 | model | 78,909 | 4 | 0 | 6 |
| 12 | competitor | fc | 5 | model | 94,262 | 5 | 0 | 10 |

（4 例分流至 legacy：范围门按意图判定，属既定设计 D5，不计入 A11。）

### 6.2 关键观察

1. **曾经的失败样本（#7，制造业物联网竞品对比）这次跑完了** —— 12 轮 / 17 次工具调用 / **316,417** tokens，**模型自主终止**、计划全闭合。它正是上一轮在 200k 被掐断的那一例。**熔断根因确认为预算过紧，非模型能力。**
2. **自然消耗分布**（全部 `stopped_by=model`，未受截断）：solution 63k–95k；competitor 78k–**316k**。中位数仅 83k —— 绝大多数任务离 200k 很远，**预算是长尾任务才碰得到的天花板**。
3. **闭合门 0 次拦截**：样本充足时模型自发闭合，宿主无需介入（对比首轮 n=5 曾拦截 1 次）。

### 6.3 预算定档（数据驱动）

`AGENT_TOKEN_BUDGET`：200000 → **500000**。

| 依据 | 值 |
|---|---|
| 实测峰值（#7） | 316,417 |
| 轮次上限推算（16 轮 × 实测 ~26k/轮） | ≈ 420,000 |
| 选定值 | **500,000**（高于峰值 58%，对齐轮次上限） |

设计意图：让**轮次**（`AGENT_MAX_TURNS=16`）成为真正的约束，token 预算退化为「只兜住病态单轮膨胀」的最后一道闸 —— 这样熔断行为可预测，不会再随机掐断正常长任务。

> 备注：本次复验在 400k 下已完成且 0 熔断（模型在 ≤316k 处自行停手）；升到 500k 只是把天花板抬到**必然高于**任何合法运行，无需重复消耗复验 —— 模型自主终止的样本不受天花板抬高影响。

---

## 7. 结论与后续选项

- ✅ **P0 目标达成**：计划从「叙述性」变成「有齿的待办」，且**齿是模型自己咬合的** —— 扩测后 `A11 主口径 1.000（8/8）`，样本充足时宿主**无需介入**（0 次拦截）。
- ✅ **熔断根因已消除**：A11 首轮 0.750 的唯一失败＝预算过紧掐断长任务；已重标定 200k → **500k**，扩测 **0 熔断**。曾经的失败样本（#7）在放宽预算后跑完全程并自主闭合。
- ⚠️ **当时仍未做的（已在 §8 全部关闭）**：① 样本仍限于 `solution`/`competitor` 两类意图（范围门未开，属 P1）；② 4 例分流 legacy 的样本不计入 A11，故 A11 尚未覆盖全部意图；③ 天花板抬到 500k 后需留意账单。
- 可选后续（**未做**，供决策）：
  - **按意图分档**：实测 solution ≤95k、competitor ≤316k，两档差异显著。若要收紧成本，可让 solution 用 200k、competitor 用 500k（`run_loop` 可直读 `harness._intent`，无需改函数签名）。当前取**单一 500k** 是为少一条代码路径、行为可预测 —— 代价是轻量任务也拿到高天花板。
  - (b) 熔断时若计划未闭合则**有界**延期（须防死锁）—— 在预算已不轻易触发的前提下优先级下降。
- 回退：`AGENT_TOKEN_BUDGET=200000`（回到旧档）；`AGENT_PLAN_CLOSE_ENFORCE=0`（闭合门整体关）；FC 本身仍可用 `AGENT_RUNTIME=legacy` 或前端 ⚡ 开关双门关闭。
- **成本提示**：500k 是天花板不是均值 —— 实测中位数仅 83k，故典型任务成本不变；仅长尾任务（如 #7 的 316k）会比旧档多用约 1.6 倍 token。

---

## 8. P1 复验：范围门放开后 0 回退（2026-09-13 收尾）

§7 遗留的三件事（范围门未开、A11 未覆盖全意图、成本分档未做）在本轮一并关闭。过程中发现并修掉一个**掩盖在"偶发回退"下的真 bug**，详见设计文档 §5.2。

### 8.1 三件事的处置

| 遗留项 | 处置 |
|---|---|
| 范围门只放 `solution`/`competitor` | **已放开**：`fc_takes_over(intent)` 改为 allowlist —— 除 `greeting`/`account`/`export`（确定性/瞬时路径）外全进 FC |
| 分类错误会带偏交付姿态（旧门的存在理由） | 交付姿态改为**按意图切换**（`build_policy_prompt(intent)` + `_CONTRACTS`），门从"内容判断"降级为"路径判断"；通用块加"姿态以用户实际诉求为准"兜底 |
| 按意图分档未做 | **已做**：solution 200k / competitor 500k（`AGENT_TOKEN_BUDGET_BY_INTENT`，env 可覆盖） |

### 8.2 复验结果（n=30，真 DeepSeek）

| 指标 | 修复前（`p1_final30.log`） | 修复后（`p1_final30_fixed.log`） |
|---|---|---|
| 进入 FC | 22 / 30 | **30 / 30** |
| FC 失败回退 | **8**（全部 `run_loop_none`） | **0** |
| 主循环 400 | 18 次（9 对 · `MAX_RETRIES=2`） | **0** |
| A11 计划收敛率 | 1.000（13/13） | **1.000（21/21）** |
| 意图覆盖 | solution / competitor | **solution 18 · general 6 · competitor 4 · knowledge_q 2，legacy 全 0** |

**真因（一句话）**：DeepSeek thinking 模式下，历史里每个 assistant 消息必须带 `reasoning_content` 字段（值可为空串），而项目把它剥掉了 → 决策轮多轮时随机 400 → `run_loop_none` → 静默回退 legacy。**不是**先前判断的"收口偶发空 content"。

### 8.3 预算分档的实测（修复后重新标定）

| 意图 | n | min | median | max | 档位 | 余量 |
|---|---|---|---|---|---|---|
| solution | 18 | 7,874 | 87,775 | 133,921 | 200,000 | 1.5× |
| competitor | 4 | 67,607 | 209,376 | **384,311** | 500,000 | 1.3× |
| general | 6 | 4,816 | 6,821 | 81,822 | 500,000 | 6.1× |
| knowledge_q | 2 | 37,270 | 55,447 | 73,624 | 500,000 | 6.8× |

**诚实口径**：分档最初基于**修复前**数据（reasoning 不回传 → 输入 token 偏低）。修复后 reasoning 随每轮回传，solution 峰值 ≤95k → **133,921（+41%）**、competitor 316k → **384,311（+22%）**。两档仍有余量（1.5× / 1.3×），故**不调档**；但这属于"在坏数据上定的档、修复后重新验过"，不算独立调参成果。

### 8.4 排障资产（留给下次）

- `schema.message_shape()`：消息序列形状签名 + `!!orphan_tool_calls@[idx]` 标记。
- `runner` 两条 `logger.warning` 落盘诊断（主循环异常 / 收口失败），含 `turn/thinking/msgs/safe_len/shape/err`。**动因**：旧代码失败路径只走 `harness._log`，受 `verbose` 门控**不落盘**，导致"失败了但不知道原因"。
- `.workbuddy/Temp/llm_400_probe.py`、`reasoning_probe*.py`：可直接复用的 API 契约探针。
- `models/llm.py`：`NonRetryableLLMError` + `_is_retryable()`，修掉"4xx 被重试"的违约行为（每次非法请求白烧 2s + 2 倍计费）。
- 离线套件 98/98（T15 重试策略 / T16 形状检测 / T17 reasoning 直通）；本地门禁 100/100 + 16/16 PASS。

### 8.5 结论

✅ P1 完成：四类意图 100% 进 FC、**零回退**、A11 1.000。范围门从"内容门"退化为"路径门"，2026-09-09 的"王者荣耀 → 14 章方案书"风险由**意图化姿态**而非"拒绝进入循环"来兜住。

⚠️ 仍未做的：① 本轮仍是**本地**运行（`127.0.0.1:8000`），**未部署 ECS**；② FC 仍非默认引擎（`AGENT_RUNTIME=legacy` 惰性），转默认需先满足设计文档的灰度门槛；③ `greeting`/`account`/`export` 走 legacy 是刻意的路径判断，不是缺口。
