# 华为云解决方案智能匹配系统 · 手机端重设计 PRD

> 版本：v1.0 ｜ 作者：产品经理（AI）｜ 日期：2026-07-15
> 状态：设计稿，待确认后进入开发

---

## 0. 背景与问题全景（来自全站移动端体检）

当前手机端（≤767px）并非"美不美"的问题，而是**系统性的功能与布局断裂**：

| 严重度 | 问题 | 影响 |
|--------|------|------|
| 🔴 高 | `.main-content{overflow:hidden}`（style.css:510）裁剪所有横向溢出 | 方案对比表、知识库表格、竞品对比表在手机上**右侧被截断不可见** |
| 🔴 高 | 全站无任何表格横向滚动包裹 | 所有宽表格在移动端系统性不可用 |
| 🔴 高 | 导航可达性断裂：底部导航隐藏"产品/成就"，且**无"设置"入口** | 产品图谱、成就中心、设置三页在手机上**完全进不去** |
| 🔴 高 | 历史记录页 `.history-tabs` 单行不换行 + 无滚动 | 历史分类 tab 在窄屏被裁剪，右侧不可点 |
| 🟠 中 | 输入框/textarea/select 字号 14px（<16px） | 聚焦时触发 iOS 自动缩放，体验割裂 |
| 🟠 中 | 顶栏：汉堡+AI栏+体验+登录挤一行 | 320px 屏拥挤；登录按钮用桌面渐变在深色顶栏突兀 |
| 🟠 中 | 底部 7 Tab 拥挤 | 图标+文字过小，误触率高 |
| 🟡 低 | 模板遗留死代码（`@media` 里 `.navbar-*`/`.mobile-menu`/`.mobile-item`/`.mobile-login-btn` 对真实 HTML 无效） | 误导维护，后续修改踩坑 |
| 🟡 低 | 多处分支 `@media` 重复/冲突定义 | 维护成本高，行为不可预测 |

**设计目标**：以"美观为核心、功能完善、使用方便、布局合理"为准则，对手机端做**信息架构重设计 + 全局规范统一 + 逐页布局美化**。

---

## 1. 设计原则（铁律）

1. **单栏流式**：手机端一律单栏，不出现双栏并排（桌面双栏在手机改为纵向堆叠）。
2. **拇指热区**：所有可点击元素最小触控尺寸 **≥ 44×44px**；底部导航置于拇指自然可达区。
3. **内容不被裁剪**：任何横向溢出内容用**横向滚动包裹**，绝不用 `overflow:hidden` 裁掉。
4. **防 iOS 缩放**：所有 `input / textarea / select` 在手机端 `font-size: 16px`。
5. **弹窗即抽屉**：手机端弹窗默认**底部上滑抽屉**（`width:100vw; border-radius:16px 16px 0 0`），关闭按钮加大。
6. **留白呼吸感**：页面边距 16px，卡片间距 12px，区块间隔 20px。
7. **字号阶梯**：页面主标题 ≤22px，区块标题 17px，正文 15–16px，辅助文字 13px，标签 12px。

---

## 2. 信息架构重设计（核心）

### 2.1 现状痛点
桌面有 7 大功能（匹配/竞品/产品/仪表盘/知识库/历史/成就）+ 设置。手机端底部塞 7 个 tab 太挤，于是粗暴隐藏"产品/成就"，又漏了"设置"——导致 3 页不可达。

### 2.2 新方案：底部 5 Tab + "我的"聚合页

**底部导航固定 5 个（拇指区，图标+文字）：**

| # | Tab | 对应页面 | 说明 |
|---|-----|----------|------|
| 1 | 匹配 | `#page-solution` | 核心功能，首屏默认 |
| 2 | 竞品 | `#page-competitor` | 核心功能 |
| 3 | 产品 | `#page-products` | 产品图谱 |
| 4 | 知识库 | `#page-knowledge` | 知识管理 |
| 5 | 我的 | `#page-mine`（**新增聚合页**） | 仪表盘/历史/成就/设置/登录 入口 |

**"我的"页（`#page-mine`）布局：**
- 顶部用户卡片：未登录 → 显示"登录/注册"大按钮 + 一句引导语；已登录 → 头像 + 用户名 + 邮箱 + 退出按钮
- 功能列表（卡片式，点击进入对应页）：
  - 📊 数据仪表盘 → `#page-dashboard`
  - 🕘 历史方案 → `#page-history`
  - 🏆 成就徽章 → `#page-achievement`
  - ⚙️ 系统设置 → `#page-settings`
- 底部：版本号 + 备案链接

> 这样 **7 大功能 + 设置 + 登录全部在手机可达**，底部 5 Tab 也不拥挤。

### 2.3 顶部栏（移动端统一规范）
```
[☰ 汉堡] [问 AI：华为云产品、方案…]        [👤登录]
```
- 汉堡键：未登录 → 直接弹登录框；已登录 → 弹用户菜单（个人/退出）
- AI 搜索栏：手机端 `max-width: 140px`，文字 0.7rem，超出省略号
- "体验"按钮：手机端**隐藏**（功能可从匹配页内访问）
- 登录按钮：透明描边极简样式（已修），已登录显示头像
- 320px 极限屏：AI 栏缩到 110px，登录按钮仅显示图标

---

## 3. 全局组件规范

### 3.1 表格（高优修复）
所有表格/对比表**必须**套横向滚动容器：
```css
.table-wrapper { overflow-x: auto; -webkit-overflow-scrolling: touch; }
.table-wrapper table { min-width: 480px; }  /* 保证列不被压扁 */
```
并修改 `.main-content`：去掉 `overflow:hidden` 裁剪（改 `overflow-x: clip` 仅裁掉非表格溢出，或对内容区放开）。受影响表格：`.kb-doc-table`、`.result-content table`、历史/竞品对比表。

### 3.2 输入框（中优修复）
```css
@media (max-width: 767px) {
  input, textarea, select { font-size: 16px !important; }
}
```
覆盖：`.textarea-input`、`.follow-up-input-area textarea`、`.setting-select`、`.kb-editor-content`、`.ai-input`。

### 3.3 弹窗（统一底部抽屉）
```css
@media (max-width: 767px) {
  .product-modal, .kb-editor-modal, .auth-modal, .history-modal-content,
  .confirm-modal, .profile-drawer {
    width: 100vw !important; max-width: 100vw !important;
    border-radius: 16px 16px 0 0 !important;
    margin: 0 !important; align-self: flex-end !important;
  }
  .product-modal-close, .ai-close-btn, .kb-editor-close, .confirm-close {
    width: 44px; height: 44px;  /* 加大点击区 */
  }
}
```

### 3.4 卡片网格
- 统计卡：3 列（知识库已做，仪表盘 KPI 2 列，统一为 2–3 列视内容）
- 产品分类：2 列
- 成就：2 列（原 4 列太挤）

### 3.5 按钮
- 主操作（开始匹配/保存/发送）：手机端**全宽**，高度 46px
- 次级操作：紧随其后或全宽次行
- 按钮组（模式切换）：横向可滚动或换行居中（已做换行，保持）

### 3.6 空状态 / Toast
- 空状态卡片：padding 24px，图标 48px，文案居中
- Toast：手机端 `max-width: 90vw`，底部居中偏上

---

## 4. 逐页布局规范

### 4.1 匹配页（`#page-solution`）
- 欢迎卡：单栏，主标题 20px，描述 14px，padding 16px
- 需求输入 textarea：高度 120px，字号 16px，全宽
- 模式切换（标准/智能/向导）：换行居中，3 个 option 等宽
- 操作按钮行：开始匹配（全宽主按钮）+ 清空（次行或并排）
- 结果区：表格套 `.table-wrapper`；来源文档横向卡片流

### 4.2 竞品页（`#page-competitor`）
- 选择区：行业/竞品 select 纵向堆叠，全宽
- 对比结果：表格套 `.table-wrapper`，列宽不压

### 4.3 产品图谱页（`#page-products`）
- 头部：标题+搜索纵向，筛选 chip 横向滚动
- 图谱区：手机端改为**分类卡片列表**（2 列），点击弹出底部抽屉详情（已做）
- 触摸提示："点击查看详情"小字引导

### 4.4 仪表盘（`#page-dashboard`）
- KPI 卡：2 列，标签允许换行（去掉 `nowrap` 截断）
- 图表卡：1 列纵向，高度收敛
- 信息条：纵向堆叠

### 4.5 知识库（`#page-knowledge`）
- 统计卡：3 列
- 文档表格：套 `.table-wrapper`，操作列可见可点
- 编辑弹窗：底部抽屉，字段全宽

### 4.6 历史记录（`#page-history`）
- 分类 tab：横向滚动（`overflow-x:auto` + `flex-wrap:nowrap`），不裁剪
- 列表项：纵向卡片，操作按钮右对齐
- 对比弹窗：底部抽屉，对比表套 `.table-wrapper`

### 4.7 成就中心（`#page-achievement`）
- 成就卡：2 列网格，描述 2 行截断
- 进度条：全宽

### 4.8 设置（`#page-settings`）
- 设置项：全宽卡片，开关 44×24
- 信息网格：2 列

### 4.9 "我的"页（`#page-mine`，新增）
- 用户卡：全宽，padding 20px
- 功能列表：4 个卡片项，点击进入
- 底部版本信息

---

## 5. 适配断点

| 断点 | 策略 |
|------|------|
| ≤767px | 手机布局（单栏、底部 5 Tab、抽屉弹窗） |
| ≤480px | 紧凑微调（Tab 文字 11px、padding 12px、KPI 标签换行） |
| ≤375px | 极限屏（AI 栏 110px、登录图标化、表格 min-width 420px） |
| ≤320px | 兜底（同上更收，保证不溢出） |

> 注：当前仅有 767px 与 600px 两个断点，需补 480/375 微调。

---

## 6. 验收标准（上线前必须逐项确认）

1. ✅ 手机端**所有 7 大功能 + 设置 + 登录均可到达**（底部 5 Tab + 我的页）
2. ✅ 任意页面宽表格可**横向滑动查看完整内容**，无截断
3. ✅ 任意输入框聚焦**不触发 iOS 缩放**
4. ✅ 顶栏在 320–414px 均不溢出、不拥挤
5. ✅ 底部导航 5 Tab 图标+文字清晰、点击区 ≥44px
6. ✅ 所有弹窗在手机上为底部抽屉、关闭按钮易点
7. ✅ 逐页卡片间距统一、无横向滚动条外的意外溢出
8. ✅ 清理模板遗留死代码 `@media` 规则，减少维护陷阱

---

## 7. 开发排期（建议顺序）

1. **P0 信息架构**：新增"我的"页 + 底部导航改 5 Tab + 路由接入
2. **P0 表格修复**：`.table-wrapper` 包裹 + 去掉 `overflow:hidden` 裁剪
3. **P1 输入框 16px** + **P1 弹窗抽屉化**
4. **P1 顶栏精简**（已完成，需补 320px 极限）
5. **P2 逐页美化**（卡片间距、网格列数、空状态）
6. **P2 死代码清理** + 补充 480/375 断点
7. 部署 + 真机验收

---

> 本 PRD 为设计基准。确认后进入开发，按 P0→P2 顺序实施，每完成一批即部署到 `cloudsol.cn` 供真机验收。
