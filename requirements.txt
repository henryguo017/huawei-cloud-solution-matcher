# LangChain生态（固定兼容版本）
langchain==0.1.20
langchain-community==0.0.38
langchain-openai==0.1.7
langchain-core==0.1.53
langchain-text-splitters==0.0.2
langsmith==0.1.147

# OpenAI SDK
openai==1.30.1

# 向量数据库（使用预编译版本，无需本地编译）
chromadb==0.4.24
chroma-hnswlib==0.7.3

# numpy：被 chromadb 0.4.24 间接依赖（传递依赖），但 app/config.py:7 顶层 import 使用，
# 故显式声明以避免传递链断导致 config 加载失败。
# 与 2.x 兼容（config 中的 shim 专为 numpy>=2 准备），但钉到 chromadb 0.4.24 兼容区间。
numpy>=1.24.0,<3.0

# 本地向量模型 (国内镜像加速)
# 注意：本地 venv 实测仅 5.x 可正常 from sentence_transformers import SentenceTransformer
# 并加载 BGE 模型；锁 2.7.0 会因模块路径变更(.base 已不存在)触发 No module named
# 'sentence_transformers.base' 警告且语义检索可能退化为零向量，故对齐到可运行版本。
sentence-transformers==5.5.1

# 文档处理
pypdf==4.2.0
python-dotenv==1.0.1

# 报告导出功能
python-docx==0.8.11
reportlab==4.0.4
jinja2==3.1.2

# 阶段1 多格式解析 + 本地 OCR（客户资料上传）
openpyxl==3.1.2
pymupdf==1.24.1
python-pptx==0.6.23
pytesseract==0.3.10

# 其他依赖
python-multipart==0.0.9
protobuf==4.25.3
packaging==23.2
tenacity==8.3.0
rich==13.7.1
pillow==10.3.0
cachetools==5.3.3

# 解决opentelemetry依赖冲突的固定版本
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp-proto-grpc==1.24.0
opentelemetry-exporter-otlp-proto-common==1.24.0
opentelemetry-proto==1.24.0
opentelemetry-instrumentation==0.45b0
opentelemetry-instrumentation-asgi==0.45b0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-semantic-conventions==0.45b0
opentelemetry-util-http==0.45b0

# FastAPI Web框架（用于RESTful API）
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# HTTP请求库
requests==2.31.0

# JWT认证
PyJWT>=2.8.0

# 密码加密
passlib[bcrypt]>=1.7.4

# 系统监控（/api/dashboard/stats 中读取进程 CPU/内存；try/except 兜底，无硬依赖）
psutil>=5.9.0

# Pydantic Email验证（Pydantic EmailStr类型必需）
email-validator>=2.1.0

# 异步HTTP客户端（LLM调用异步化）
httpx>=0.27.0

# 钉钉 Stream 模式交互机器人（独立 systemd 服务 cloudsol-im-bot；主 API 不依赖此包）
dingtalk-stream==0.24.0

# 飞书长连接交互机器人（独立 systemd 服务 cloudsol-im-feishu；主 API 不依赖此包）
lark-oapi==1.7.3
